{
  "name": "NodeDirectUploader",
  "version": "0.0.1",
  "private": true,
  "jest": {
    "setupFiles": [
      "./.env"
    ]
  },
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon 'heroku local' --signal SIGTERM",
    "test": "NODE_ENV=test jest --detectOpenHandles --runInBand",
    "test:watch": "NODE_ENV=test jest --watch --runInBand --detectOpenHandles"
  },
  "dependencies": {
    "aws-sdk": "2.x",
    "axios": "^0.19.2",
    "bootstrap": "^4.4.1",
    "ejs": "2.x",
    "express": "4.x",
    "mongoose": "^5.9.5",
    "open-graph": "^0.2.4",
    "short-uuid": "^3.1.1"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0",
    "jest": "^25.2.4",
    "nodemon": "^2.0.2",
    "supertest": "^4.0.2"
  }
}
